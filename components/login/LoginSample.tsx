"use client"

import React, { useEffect, useState } from "react"

function LoginSample() {
    // const [test, setTest] = useState(false)

    // useEffect(() => {
    //     setTimeout(() => {
    //         setTest(true)
    //     }, 1000)
    // }, [])

    // if (!test) return null

    return <div>{"LoginSample"}</div>
}

export default LoginSample
